# Whitelist: ignore everything, then re-include only what this repo ships.
*

!/.gitattributes
!/.gitignore
!/LICENSE
!/README.md

!/src/
!/src/**

# Build output that lives inside src/. dist/ is the staged mod folder, local only.
src/**/bin/
src/**/obj/
